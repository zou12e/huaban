
const initialState = {
  x: 1
};
const reducer = function (state = initialState, action) {
  return state;
};

export default reducer;

export function login() {
  return {
    type: 'EMPTYSTATU'
  };
}

